# FastAPI production runtime only.
# Training, notebooks, MLflow, visualization, testing, and Streamlit stay in
# requirements.txt and are intentionally excluded from the backend image.

fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
pydantic>=2.0,<3.0
python-dotenv>=1.0,<2.0

# Saved Logistic Regression pipeline and prepared inference table.
pandas>=2.3,<3.0
numpy>=2.3,<3.0
scikit-learn>=1.7,<2.0
joblib>=1.5,<2.0

# Both providers remain runtime-selectable. Imports are currently eager, so
# both SDKs are required even when only one provider is configured.
openai>=1.0,<2.0
google-genai>=1.0,<2.0

# Alternative Phase 9 orchestration endpoint.
langgraph>=1.2,<2.0
